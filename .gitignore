# global env
.env

# primarily for prettier cache
node_modules/

# ollama llm model data. includes api keys and large metadata files not useful to repo
ollama_data/

# set up docker locally. not relevant for project implementation
docker-setup-guide.txt